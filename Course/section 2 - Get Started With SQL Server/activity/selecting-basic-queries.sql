SELECT 1+1; --Show with No column name
SELECT 1+1 Result; --Show with column name Result

SELECT 1+1 AS Result;--Show with column name as Result
SELECT 1*1 AS Result;

SELECT 1+1 AS Result; --Show with column name as Result
GO --batch used to join 
SELECT 1*1 AS Result;